<%= form_for([@vacancy, @vacancy.reactions.build]) do |f| %>

   <p>
      <%= f.label :first_name %><br />
      <%= f.text_field :first_name %>
   </p>

   <p>
      <%= f.label :last_name %><br />
      <%= f.text_field :last_name %>
   </p>

   <p>
      <%= f.label :age %><br />
      <%= f.number_field :age %>
   </p>

   <p>
      <%= f.label :email %><br />
      <%= f.text_field :email %>
   </p>

   <p>
      <%= f.label :education %><br />
      <%= f.text_field :education %>
   </p>

   <p>
      <%= f.label :body %><br />
      <%= f.text_area :body %>
   </p>

   <p>
      <%= f.submit %>
   </p>

<% end %>
